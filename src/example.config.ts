const recipients = []; // List of email addresses as an array
const subject = ""; // Subject header of the email
const htmlContent = ``; // HTML content of your email goes here

export { recipients, subject, htmlContent };
